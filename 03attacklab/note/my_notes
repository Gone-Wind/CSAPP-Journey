level4:
    增加了防御措施, 随机化 和 堆栈内部代码无法执行 -> 可通过面向返回编程解决ROP
    
    还是可以通过getbuf输入字符串修改原栈的内容

    可通过现有代码块 gadget -> 构建level2所需的代码块

    level2所需执行代码块为:
        movq  $0x59b997fa,%rdi
        pushq $0x4017ec
        ret

level5:
    传递字符串cookies,要考虑栈随机化的影响
